# C++ 引用 (&) 和右值引用 (&&) 详解

## 问题代码

```cpp
// Copy constructor (拷贝构造函数)
SmartPointer(const SmartPointer& p) noexcept 
    : m_Pointer(p.m_Pointer) 
{
    this->Register();
}

// Move constructor (移动构造函数)
SmartPointer(SmartPointer<ObjectType>&& p) noexcept
    : m_Pointer(p.m_Pointer)
{
    p.m_Pointer = nullptr;
}
```

## 关键区别

### 1. `&` - 左值引用（Lvalue Reference）

```cpp
SmartPointer(const SmartPointer& p)
//                        ↑
//                    单个 & = 左值引用
```

**含义**：
- 引用一个**左值**（有名字的变量）
- 可以多次使用
- 拷贝构造函数使用这个

### 2. `&&` - 右值引用（Rvalue Reference）

```cpp
SmartPointer(SmartPointer<ObjectType>&& p)
//                              ↑↑
//                          两个 && = 右值引用
```

**含义**：
- 引用一个**右值**（临时对象、即将销毁的对象）
- 只能使用一次
- 移动构造函数使用这个

## 什么是左值和右值？

### 左值（Lvalue）

**定义**：有名字的变量，可以取地址

```cpp
SmartPointer<Object> obj = Object::New();  // obj 是左值
SmartPointer<Object> obj2 = obj;           // obj 是左值，可以多次使用
```

**特点**：
- 有名字
- 可以取地址 `&obj`
- 可以多次使用
- 生命周期较长

### 右值（Rvalue）

**定义**：临时对象、字面量、即将销毁的对象

```cpp
// 示例1：函数返回值（临时对象）
SmartPointer<Object> GetObject() {
    return Object::New();  // 返回值是右值
}

SmartPointer<Object> obj = GetObject();  // GetObject() 返回的是右值

// 示例2：std::move 的结果
SmartPointer<Object> obj1 = Object::New();
SmartPointer<Object> obj2 = std::move(obj1);  // std::move(obj1) 是右值

// 示例3：字面量
int x = 10;  // 10 是右值
```

**特点**：
- 没有名字（或即将销毁）
- 不能取地址
- 只能使用一次
- 生命周期很短

## 拷贝构造函数 vs 移动构造函数

### 拷贝构造函数（Copy Constructor）

```cpp
SmartPointer(const SmartPointer& p) noexcept 
    : m_Pointer(p.m_Pointer) 
{
    this->Register();  // 增加引用计数
}
```

**使用场景**：
```cpp
SmartPointer<Object> obj1 = Object::New();
SmartPointer<Object> obj2 = obj1;  // 调用拷贝构造函数
// obj1 仍然有效，可以继续使用
```

**特点**：
- 参数是 `const SmartPointer&`（左值引用）
- **不修改**原对象
- **增加引用计数**（两个指针都引用同一对象）
- 原对象仍然有效

**内存效果**：
```
obj1 → [Object] ← obj2
       ↑
    两个指针都引用同一个对象
    引用计数 = 2
```

### 移动构造函数（Move Constructor）

```cpp
SmartPointer(SmartPointer<ObjectType>&& p) noexcept
    : m_Pointer(p.m_Pointer)
{
    p.m_Pointer = nullptr;  // 清空原指针
}
```

**使用场景**：
```cpp
SmartPointer<Object> obj1 = Object::New();
SmartPointer<Object> obj2 = std::move(obj1);  // 调用移动构造函数
// obj1 被"移动"了，不应该再使用
```

**特点**：
- 参数是 `SmartPointer&&`（右值引用）
- **修改**原对象（清空指针）
- **不改变引用计数**（只是转移所有权）
- 原对象被"掏空"，不应该再使用

**内存效果**：
```
移动前：
obj1 → [Object]
obj2 → (空)

移动后：
obj1 → nullptr (被清空)
obj2 → [Object] (所有权转移)
```

## 为什么需要两个构造函数？

### 场景1：需要拷贝（使用左值引用）

```cpp
SmartPointer<Object> obj1 = Object::New();
SmartPointer<Object> obj2 = obj1;  // 调用拷贝构造函数

// obj1 和 obj2 都引用同一个对象
// 引用计数 = 2
// obj1 仍然有效，可以继续使用
```

**为什么用拷贝**：
- `obj1` 还要继续使用
- 需要两个独立的引用
- 增加引用计数

### 场景2：需要移动（使用右值引用）

```cpp
SmartPointer<Object> CreateObject() {
    return Object::New();  // 返回临时对象（右值）
}

SmartPointer<Object> obj = CreateObject();  // 调用移动构造函数

// CreateObject() 返回的是临时对象，即将销毁
// 不需要拷贝，直接"移动"所有权
// 更高效！
```

**为什么用移动**：
- 原对象是临时的，即将销毁
- 不需要拷贝，直接转移所有权
- 更高效（避免不必要的引用计数操作）

## 实际代码示例

### 示例1：拷贝构造函数

```cpp
void ExampleCopy() {
    SmartPointer<Object> obj1 = Object::New();
    std::cout << "obj1 引用计数: " << obj1->GetReferenceCount() << std::endl;
    // 输出：1
    
    SmartPointer<Object> obj2 = obj1;  // 调用拷贝构造函数
    std::cout << "obj1 引用计数: " << obj1->GetReferenceCount() << std::endl;
    std::cout << "obj2 引用计数: " << obj2->GetReferenceCount() << std::endl;
    // 输出：2, 2（两个指针引用同一对象）
    
    // obj1 仍然有效
    obj1->SetName("MyObject");
    std::cout << obj2->GetName() << std::endl;  // 输出：MyObject
}
```

### 示例2：移动构造函数

```cpp
SmartPointer<Object> CreateObject() {
    SmartPointer<Object> obj = Object::New();
    return obj;  // 返回临时对象（右值）
}

void ExampleMove() {
    SmartPointer<Object> obj = CreateObject();  // 调用移动构造函数
    
    // CreateObject() 返回的临时对象被"移动"了
    // 不需要拷贝，直接转移所有权
    // 更高效！
}
```

### 示例3：std::move 显式移动

```cpp
void ExampleExplicitMove() {
    SmartPointer<Object> obj1 = Object::New();
    
    // 显式移动
    SmartPointer<Object> obj2 = std::move(obj1);  // 调用移动构造函数
    
    // obj1 被"掏空"，不应该再使用
    // obj1.m_Pointer = nullptr
    
    // obj2 拥有对象的所有权
    obj2->SetName("MovedObject");
}
```

## 编译器如何选择？

### 规则

1. **左值** → 调用**拷贝构造函数**
   ```cpp
   SmartPointer<Object> obj1 = Object::New();
   SmartPointer<Object> obj2 = obj1;  // 左值，调用拷贝构造函数
   ```

2. **右值** → 调用**移动构造函数**
   ```cpp
   SmartPointer<Object> obj = CreateObject();  // 右值，调用移动构造函数
   SmartPointer<Object> obj2 = std::move(obj1);  // 右值，调用移动构造函数
   ```

### 优先级

- 如果**有移动构造函数**，右值优先使用移动
- 如果**没有移动构造函数**，右值会使用拷贝（向后兼容）

## 性能对比

### 拷贝构造函数

```cpp
SmartPointer(const SmartPointer& p) noexcept 
    : m_Pointer(p.m_Pointer) 
{
    this->Register();  // 原子操作，增加引用计数
}
```

**开销**：
- 原子操作（引用计数+1）
- 两个指针都引用同一对象
- 原对象仍然有效

### 移动构造函数

```cpp
SmartPointer(SmartPointer<ObjectType>&& p) noexcept
    : m_Pointer(p.m_Pointer)
{
    p.m_Pointer = nullptr;  // 简单赋值
}
```

**开销**：
- 只是指针赋值
- 不改变引用计数
- 更高效！

## 总结

### 关键区别

| 特性 | `&` (左值引用) | `&&` (右值引用) |
|------|--------------|---------------|
| **符号** | 单个 `&` | 两个 `&&` |
| **引用对象** | 左值（有名字的变量） | 右值（临时对象） |
| **使用次数** | 可以多次使用 | 只能使用一次 |
| **构造函数** | 拷贝构造函数 | 移动构造函数 |
| **是否修改原对象** | 不修改 | 修改（清空） |
| **引用计数** | 增加 | 不变 |
| **性能** | 较慢（原子操作） | 较快（简单赋值） |

### 记忆方法

1. **`&` = 引用（Reference）**
   - 单个 `&` = 左值引用
   - 用于拷贝

2. **`&&` = 移动（Move）**
   - 两个 `&&` = 右值引用
   - 用于移动

3. **左值用拷贝，右值用移动**
   - 有名字的变量 → 拷贝
   - 临时对象 → 移动

### 实际应用

```cpp
// 拷贝：两个指针都引用同一对象
SmartPointer<Object> obj1 = Object::New();
SmartPointer<Object> obj2 = obj1;  // 拷贝，引用计数+1

// 移动：转移所有权
SmartPointer<Object> obj3 = std::move(obj1);  // 移动，obj1 被清空
```
