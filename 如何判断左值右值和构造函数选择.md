# 如何判断左值/右值，以及编译器如何选择构造函数

## 问题

1. 我怎么知道想要的是左值引用还是右值引用？
2. 为什么写两个 `&&` 就知道调用的是右值引用的构造函数？

## 答案：编译器自动判断，你不需要手动选择！

### 关键点：编译器根据表达式类型自动选择

```cpp
// 你有两个构造函数：
SmartPointer(const SmartPointer& p);      // 拷贝构造函数（左值引用）
SmartPointer(SmartPointer&& p);          // 移动构造函数（右值引用）

// 编译器会根据你写的代码自动选择：
SmartPointer<Object> obj1 = Object::New();
SmartPointer<Object> obj2 = obj1;        // 编译器：obj1 是左值 → 调用拷贝构造函数
SmartPointer<Object> obj3 = std::move(obj1);  // 编译器：std::move(obj1) 是右值 → 调用移动构造函数
```

## 如何判断左值和右值？

### 简单规则

1. **有名字的变量 = 左值**
   ```cpp
   SmartPointer<Object> obj = Object::New();
   // obj 有名字 → 左值
   ```

2. **临时对象 = 右值**
   ```cpp
   Object::New()  // 返回值是临时对象 → 右值
   ```

3. **函数返回值 = 右值**（通常）
   ```cpp
   SmartPointer<Object> CreateObject() {
       return Object::New();  // 返回值是右值
   }
   ```

4. **std::move() 的结果 = 右值**
   ```cpp
   std::move(obj)  // 强制转换为右值
   ```

## 编译器如何选择构造函数？

### 重载解析规则

编译器会尝试匹配所有可用的构造函数，选择最合适的：

```cpp
class SmartPointer {
public:
    // 构造函数1：接受左值引用
    SmartPointer(const SmartPointer& p);
    
    // 构造函数2：接受右值引用
    SmartPointer(SmartPointer&& p);
};
```

### 匹配规则

1. **如果表达式是左值** → 匹配 `const SmartPointer&`
2. **如果表达式是右值** → 匹配 `SmartPointer&&`

## 实际例子

### 例子1：左值 → 调用拷贝构造函数

```cpp
SmartPointer<Object> obj1 = Object::New();
SmartPointer<Object> obj2 = obj1;  // obj1 是左值

// 编译器分析：
// 1. obj1 有名字 → 左值
// 2. 尝试匹配构造函数：
//    - SmartPointer(const SmartPointer& p)  ← 匹配！(左值引用)
//    - SmartPointer(SmartPointer&& p)       ← 不匹配（需要右值）
// 3. 选择拷贝构造函数 ✅
```

### 例子2：右值 → 调用移动构造函数

```cpp
SmartPointer<Object> obj = Object::New();  // Object::New() 返回右值

// 编译器分析：
// 1. Object::New() 返回临时对象 → 右值
// 2. 尝试匹配构造函数：
//    - SmartPointer(const SmartPointer& p)  ← 可以匹配（但优先级低）
//    - SmartPointer(SmartPointer&& p)      ← 完美匹配！（右值引用，优先级高）
// 3. 选择移动构造函数 ✅（因为右值引用优先级更高）
```

### 例子3：std::move() → 强制调用移动构造函数

```cpp
SmartPointer<Object> obj1 = Object::New();
SmartPointer<Object> obj2 = std::move(obj1);  // std::move(obj1) 是右值

// 编译器分析：
// 1. std::move(obj1) 返回右值引用 → 右值
// 2. 尝试匹配构造函数：
//    - SmartPointer(const SmartPointer& p)  ← 可以匹配（但优先级低）
//    - SmartPointer(SmartPointer&& p)      ← 完美匹配！（右值引用，优先级高）
// 3. 选择移动构造函数 ✅
```

## std::move() 的作用

### std::move() 是什么？

```cpp
template<typename T>
constexpr typename std::remove_reference<T>::type&& move(T&& t) noexcept {
    return static_cast<typename std::remove_reference<T>::type&&>(t);
}
```

**简单理解**：`std::move()` 把左值强制转换为右值

### 为什么需要 std::move()？

```cpp
SmartPointer<Object> obj1 = Object::New();

// 没有 std::move()：
SmartPointer<Object> obj2 = obj1;  
// obj1 是左值 → 调用拷贝构造函数
// obj1 仍然有效，引用计数+1

// 有 std::move()：
SmartPointer<Object> obj2 = std::move(obj1);
// std::move(obj1) 是右值 → 调用移动构造函数
// obj1 被"移动"，指针被清空，引用计数不变
```

### std::move() 的实际效果

```cpp
SmartPointer<Object> obj1 = Object::New();

// 情况1：不使用 std::move()
SmartPointer<Object> obj2 = obj1;
// 编译器看到：obj1 是左值
// 选择：拷贝构造函数
// 结果：obj1 仍然有效，引用计数+1

// 情况2：使用 std::move()
SmartPointer<Object> obj2 = std::move(obj1);
// 编译器看到：std::move(obj1) 是右值
// 选择：移动构造函数
// 结果：obj1 被清空，所有权转移
```

## 为什么写 `&&` 就知道是右值引用？

### 类型匹配规则

```cpp
// 构造函数定义
SmartPointer(SmartPointer&& p);  // 参数类型是右值引用

// 调用时
SmartPointer<Object> obj2 = std::move(obj1);
// std::move(obj1) 的类型是右值引用
// 匹配 SmartPointer&& 参数 ✅
```

### 编译器的工作流程

```cpp
// 1. 你写的代码
SmartPointer<Object> obj2 = std::move(obj1);

// 2. 编译器分析表达式类型
// std::move(obj1) 的类型是：SmartPointer<Object>&&（右值引用）

// 3. 编译器查找匹配的构造函数
// 构造函数1：SmartPointer(const SmartPointer& p)
//           参数类型：const SmartPointer&（左值引用）
//           不匹配 ❌

// 构造函数2：SmartPointer(SmartPointer&& p)
//           参数类型：SmartPointer&&（右值引用）
//           完美匹配 ✅

// 4. 选择移动构造函数
```

## 实际代码示例

### 示例1：自动选择拷贝构造函数

```cpp
void Example1() {
    SmartPointer<Object> obj1 = Object::New();
    
    // obj1 是左值（有名字）
    SmartPointer<Object> obj2 = obj1;  
    // 编译器：obj1 是左值 → 调用拷贝构造函数
    // 结果：obj1 和 obj2 都引用同一对象，引用计数=2
}
```

### 示例2：自动选择移动构造函数

```cpp
SmartPointer<Object> CreateObject() {
    return Object::New();  // 返回临时对象（右值）
}

void Example2() {
    // CreateObject() 返回右值
    SmartPointer<Object> obj = CreateObject();
    // 编译器：CreateObject() 是右值 → 调用移动构造函数
    // 结果：直接移动，不需要拷贝
}
```

### 示例3：显式使用 std::move()

```cpp
void Example3() {
    SmartPointer<Object> obj1 = Object::New();
    
    // std::move(obj1) 强制转换为右值
    SmartPointer<Object> obj2 = std::move(obj1);
    // 编译器：std::move(obj1) 是右值 → 调用移动构造函数
    // 结果：obj1 被清空，所有权转移给 obj2
}
```

## 如何知道该用哪个？

### 一般规则

1. **默认情况**：让编译器自动选择
   ```cpp
   SmartPointer<Object> obj = Object::New();  // 自动选择移动（右值）
   SmartPointer<Object> obj2 = obj;          // 自动选择拷贝（左值）
   ```

2. **需要移动时**：使用 `std::move()`
   ```cpp
   SmartPointer<Object> obj1 = Object::New();
   SmartPointer<Object> obj2 = std::move(obj1);  // 显式移动
   ```

3. **需要拷贝时**：直接赋值（不使用 `std::move()`）
   ```cpp
   SmartPointer<Object> obj1 = Object::New();
   SmartPointer<Object> obj2 = obj1;  // 显式拷贝
   ```

### 判断标准

| 情况 | 使用 | 结果 |
|------|------|------|
| 原对象还要继续使用 | 直接赋值（拷贝） | 调用拷贝构造函数 |
| 原对象不再使用 | `std::move()`（移动） | 调用移动构造函数 |
| 函数返回值 | 直接赋值 | 自动移动（右值） |

## 总结

### 关键点

1. **编译器自动判断**
   - 你不需要手动选择
   - 编译器根据表达式类型自动选择构造函数

2. **左值 → 拷贝，右值 → 移动**
   - 有名字的变量 = 左值 → 拷贝构造函数
   - 临时对象/函数返回值 = 右值 → 移动构造函数

3. **`&&` 表示右值引用**
   - 写 `&&` 就是告诉编译器：这个构造函数接受右值
   - 编译器看到右值表达式时，会自动匹配这个构造函数

4. **`std::move()` 的作用**
   - 把左值强制转换为右值
   - 强制调用移动构造函数

### 记忆方法

- **左值有名字，右值是临时**
- **左值用拷贝，右值用移动**
- **想要移动用 `std::move()`**

### 实际使用

```cpp
// 大多数情况下，让编译器自动选择：
SmartPointer<Object> obj = Object::New();  // 自动移动（右值）
SmartPointer<Object> obj2 = obj;            // 自动拷贝（左值）

// 需要显式移动时：
SmartPointer<Object> obj3 = std::move(obj); // 强制移动
```
