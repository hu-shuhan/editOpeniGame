This folder contains the tensor-based subdivision implementation. 

The supported XML input defines `TensorBSpline3` geometries, including three parametric B-spline bases with their degrees and knot vectors, 3D control-point coefficients (`coefs`), and associated scalar data (`scalars`).