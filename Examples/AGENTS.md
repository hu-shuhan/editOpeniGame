# 回归测试说明

新增或修改回归测试时，在测试文件或对应用例前写注释，说明：

- 当时出现的 BUG、触发条件和错误表现。
- 测试要保证的正确行为，以及重要边界用例的原因。
- 修复提交的真实提交号；尚未提交时明确写“待提交”，不能编造。测试与修复同次首次提交时，为避免提交号自引用，记录准确的提交主题，以及用 `git log --diff-filter=A --format="%h %s" -- <测试路径>` 查询该提交的命令。

这一要求来自用户在 2026-09-19 对点数据动画显示问题的反馈。
