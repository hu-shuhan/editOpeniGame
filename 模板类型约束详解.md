# C++ 模板类型约束详解

## 问题代码

```cpp
template <typename T, typename = typename EnableIfConvertible<T>::type>
SmartPointer(const SmartPointer<T>& p) noexcept
```

## 逐步分解

### 1. 基本结构

```cpp
template <typename T, typename = ...>
```
- `template`：模板声明
- `typename T`：第一个模板参数，类型 `T`
- `typename = ...`：第二个模板参数，有默认值，用于类型约束

### 2. EnableIfConvertible 的定义

```cpp
template <typename T>
using EnableIfConvertible = typename std::enable_if<std::is_convertible_v<T*, TObjectType*>>;
```

让我们分解这个定义：

#### std::is_convertible_v<T*, TObjectType*>
- **作用**：编译时检查类型转换是否合法
- **检查内容**：`T*` 能否转换为 `TObjectType*`
- **返回值**：`true` 或 `false`（编译时常量）

**示例**：
```cpp
// 假设 TObjectType = DataObject
// T = PointSet（PointSet 继承自 DataObject）

std::is_convertible_v<PointSet*, DataObject*>  // true（向上转换）
std::is_convertible_v<DataObject*, PointSet*>   // false（向下转换需要 dynamic_cast）
```

#### std::enable_if<条件>
- **作用**：只有当条件为 `true` 时，才定义 `type`
- **如果条件为 `true`**：`std::enable_if<true>::type` 存在（类型是 `void`）
- **如果条件为 `false`**：`std::enable_if<false>::type` **不存在**（编译错误）

**示例**：
```cpp
// 条件为 true
std::enable_if<true>::type  // 存在，类型是 void

// 条件为 false
std::enable_if<false>::type  // 不存在，编译错误
```

#### typename EnableIfConvertible<T>::type
- **作用**：获取 `EnableIfConvertible<T>` 的 `type` 成员
- **如果 `T*` 可转换为 `TObjectType*`**：`type` 存在（类型是 `void`）
- **如果 `T*` 不可转换**：`type` 不存在，模板实例化失败

### 3. 完整的模板参数

```cpp
template <typename T, typename = typename EnableIfConvertible<T>::type>
```

**含义**：
- 第二个模板参数有默认值：`typename EnableIfConvertible<T>::type`
- **只有当 `T*` 可以转换为 `TObjectType*` 时**，这个默认值才存在
- **如果转换不合法**，模板实例化失败（SFINAE 机制）

## SFINAE 机制

**SFINAE** = **S**ubstitution **F**ailure **I**s **N**ot **A**n **E**rror（替换失败不是错误）

### 工作原理

```cpp
// 场景1：T* 可以转换为 TObjectType*
SmartPointer<PointSet> ps = PointSet::New();
SmartPointer<DataObject> obj = ps;  // ✅ 编译成功
// 因为 PointSet* 可以转换为 DataObject*（向上转换）

// 场景2：T* 不能转换为 TObjectType*
SmartPointer<DataObject> obj = DataObject::New();
SmartPointer<PointSet> ps = obj;  // ❌ 编译失败
// 因为 DataObject* 不能直接转换为 PointSet*（需要 dynamic_cast）
```

### 编译过程

```cpp
// 当编译器看到：
SmartPointer<DataObject> obj = ps;  // ps 是 SmartPointer<PointSet>

// 编译器尝试实例化：
template <typename T, typename = typename EnableIfConvertible<T>::type>
SmartPointer(const SmartPointer<T>& p)

// 1. T = PointSet
// 2. 检查：std::is_convertible_v<PointSet*, DataObject*>
// 3. 结果：true
// 4. EnableIfConvertible<PointSet>::type 存在
// 5. 模板实例化成功 ✅

// 如果尝试：
SmartPointer<PointSet> ps = obj;  // obj 是 SmartPointer<DataObject>

// 1. T = DataObject
// 2. 检查：std::is_convertible_v<DataObject*, PointSet*>
// 3. 结果：false
// 4. EnableIfConvertible<DataObject>::type 不存在
// 5. 模板实例化失败 ❌（但这是 SFINAE，不是错误，只是这个模板不匹配）
```

## 实际应用场景

### 场景1：允许向上转换（派生类 → 基类）

```cpp
// 类继承关系
class DataObject : public Object { ... };
class PointSet : public DataObject { ... };
class SurfaceMesh : public PointSet { ... };

// 使用示例
PointSet::Pointer pointSet = PointSet::New();
DataObject::Pointer dataObj = pointSet;  // ✅ 允许（向上转换）

SurfaceMesh::Pointer mesh = SurfaceMesh::New();
PointSet::Pointer ps = mesh;  // ✅ 允许（向上转换）
DataObject::Pointer obj = mesh;  // ✅ 允许（向上转换）
```

### 场景2：禁止向下转换（基类 → 派生类）

```cpp
DataObject::Pointer obj = DataObject::New();
PointSet::Pointer ps = obj;  // ❌ 编译错误（向下转换不允许）

// 必须使用 DynamicCast：
PointSet::Pointer ps = DynamicCast<PointSet>(obj);  // ✅ 使用运行时检查
```

## 完整代码流程

```cpp
// 1. 定义类型别名
template <typename T>
using EnableIfConvertible = typename std::enable_if<std::is_convertible_v<T*, TObjectType*>>;

// 2. 使用在模板构造函数中
template <typename T, typename = typename EnableIfConvertible<T>::type>
SmartPointer(const SmartPointer<T>& p) noexcept
    : m_Pointer(p.m_Pointer)
{
    this->Register();
}

// 3. 编译时检查
// 如果 T* 可以转换为 TObjectType*：
//   - EnableIfConvertible<T>::type 存在
//   - 模板实例化成功
//   - 构造函数可用

// 如果 T* 不能转换为 TObjectType*：
//   - EnableIfConvertible<T>::type 不存在
//   - 模板实例化失败（SFINAE）
//   - 编译器尝试其他构造函数或报错
```

## 等价写法（C++20 之前）

### 传统写法（C++11/14）

```cpp
template <typename T>
using EnableIfConvertible = typename std::enable_if<
    std::is_convertible<T*, TObjectType*>::value
>::type;

template <typename T, typename = EnableIfConvertible<T>>
SmartPointer(const SmartPointer<T>& p) noexcept
```

### 现代写法（C++17）

```cpp
template <typename T>
using EnableIfConvertible = typename std::enable_if<
    std::is_convertible_v<T*, TObjectType*>
>::type;

template <typename T, typename = EnableIfConvertible<T>>
SmartPointer(const SmartPointer<T>& p) noexcept
```

### 最简洁写法（C++20）

```cpp
// C++20 引入了 concepts，可以这样写：
template <typename T>
concept ConvertibleToBase = std::is_convertible_v<T*, TObjectType*>;

template <ConvertibleToBase T>
SmartPointer(const SmartPointer<T>& p) noexcept
```

## 为什么这样设计？

### 1. 类型安全
- **编译时检查**：确保只有合法的类型转换才能编译通过
- **防止错误**：避免运行时类型转换错误

### 2. 灵活性
- **支持向上转换**：派生类可以自动转换为基类
- **禁止向下转换**：基类不能直接转换为派生类（需要 `DynamicCast`）

### 3. 性能
- **零运行时开销**：所有检查都在编译时完成
- **无额外代码**：SFINAE 失败不会生成任何代码

## 总结

```cpp
template <typename T, typename = typename EnableIfConvertible<T>::type>
```

**这句话的意思是**：
1. 定义一个模板构造函数，接受 `SmartPointer<T>` 类型
2. **只有当 `T*` 可以转换为 `TObjectType*` 时**，这个构造函数才可用
3. 这是**编译时类型检查**，确保类型安全
4. 使用 **SFINAE** 机制，失败时不会报错，只是这个模板不匹配

**实际效果**：
- ✅ 允许：`SmartPointer<基类> = SmartPointer<派生类>`
- ❌ 禁止：`SmartPointer<派生类> = SmartPointer<基类>`
