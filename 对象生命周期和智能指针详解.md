# iGameVis 对象生命周期和智能指针详解

## 目录
1. [概述](#概述)
2. [I_OBJECT 宏](#i_object-宏)
3. [AbstractObject - 引用计数基础](#abstractobject---引用计数基础)
4. [SmartPointer - 智能指针实现](#smartpointer---智能指针实现)
5. [Object - 完整对象系统](#object---完整对象系统)
6. [使用示例](#使用示例)
7. [设计模式分析](#设计模式分析)

---

## 概述

iGameVis 框架使用**引用计数（Reference Counting）**机制来管理对象生命周期，这是一个类似 `std::shared_ptr` 但更轻量级的实现。核心思想是：

- **每个对象维护一个引用计数**
- **当对象被引用时，计数+1**
- **当引用失效时，计数-1**
- **当计数为0时，自动删除对象**

这种设计避免了手动内存管理，防止内存泄漏和悬空指针。

---

## I_OBJECT 宏

### 定义（`iGameMacro.h`）

```cpp
#define I_OBJECT(TypeName)                                                                                             \
    TypeName(const TypeName&) = delete;                                                                                \
    TypeName& operator=(const TypeName&) = delete;                                                                     \
    TypeName(TypeName&&) = delete;                                                                                     \
    TypeName& operator=(TypeName&&) = delete;                                                                          \
    using Self = TypeName;                                                                                             \
    using Pointer = SmartPointer<Self>;                                                                                \
    using ConstPointer = SmartPointer<const Self>;
```

### 作用解析

1. **禁止拷贝和移动**：
   - `delete` 拷贝构造函数和赋值运算符
   - `delete` 移动构造函数和移动赋值运算符
   - **原因**：对象只能通过智能指针管理，不允许直接拷贝

2. **类型别名**：
   - `Self` = 当前类型
   - `Pointer` = `SmartPointer<Self>` - 智能指针类型
   - `ConstPointer` = `SmartPointer<const Self>` - 常量智能指针

### 使用示例

```cpp
class MyClass : public Object {
public:
    I_OBJECT(MyClass);  // 展开后：
                        // - 禁止拷贝/移动
                        // - 定义 MyClass::Pointer = SmartPointer<MyClass>
                        // - 定义 MyClass::ConstPointer = SmartPointer<const MyClass>
    
    static Pointer New() { return new MyClass; }
    
protected:
    MyClass() {}
    ~MyClass() override = default;
};
```

---

## AbstractObject - 引用计数基础

### 完整代码（`iGameAbstractObject.h`）

```cpp
class AbstractObject {
public:
    I_OBJECT(AbstractObject);

    // 强制删除对象（危险操作）
    virtual void Delete() const {
        m_ReferenceCount = 0;
        delete this;
    }

    // 增加引用计数
    virtual void Register() const {
        ++m_ReferenceCount;
    }

    // 减少引用计数，如果为0则删除对象
    virtual void UnRegister() const noexcept {
        if (--m_ReferenceCount <= 0) {
            delete this;
        }
    }

protected:
    AbstractObject() {}
    virtual ~AbstractObject() {}

    // 引用计数（使用原子操作，线程安全）
    mutable std::atomic<int> m_ReferenceCount{};
};
```

### 关键点解析

#### 1. 引用计数变量
```cpp
mutable std::atomic<int> m_ReferenceCount{};
```
- **`mutable`**：允许在 `const` 函数中修改（因为 `Register/UnRegister` 是 `const` 的）
- **`std::atomic<int>`**：原子操作，保证线程安全
- **初始化为0**：新创建的对象引用计数为0

#### 2. Register() - 增加引用
```cpp
virtual void Register() const {
    ++m_ReferenceCount;
}
```
- 当对象被智能指针引用时调用
- 简单地将计数+1

#### 3. UnRegister() - 减少引用
```cpp
virtual void UnRegister() const noexcept {
    if (--m_ReferenceCount <= 0) {
        delete this;
    }
}
```
- 当智能指针失效时调用
- 计数-1，如果<=0则删除对象
- **`delete this`**：在对象内部删除自己（需要确保对象在堆上分配）

#### 4. Delete() - 强制删除
```cpp
virtual void Delete() const {
    m_ReferenceCount = 0;
    delete this;
}
```
- **危险操作**：直接删除对象，不检查引用计数
- 可能导致悬空指针，一般不使用

---

## SmartPointer - 智能指针实现

### 完整代码（`iGameSmartPointer.h`）

```cpp
template <typename TObjectType>
class SmartPointer {
public:
    using ObjectType = TObjectType;

    // 类型转换辅助
    template <typename T>
    using EnableIfConvertible = typename std::enable_if<std::is_convertible_v<T*, TObjectType*>>;

    // 默认构造函数
    constexpr SmartPointer() noexcept = default;
    constexpr SmartPointer(std::nullptr_t) noexcept {}

    // 拷贝构造函数（支持类型转换）
    template <typename T, typename = typename EnableIfConvertible<T>::type>
    SmartPointer(const SmartPointer<T>& p) noexcept
        : m_Pointer(p.m_Pointer) {
        this->Register();
    }

    // 拷贝构造函数（同类型）
    SmartPointer(const SmartPointer& p) noexcept 
        : m_Pointer(p.m_Pointer) {
        this->Register();
    }

    // 移动构造函数
    SmartPointer(SmartPointer<ObjectType>&& p) noexcept
        : m_Pointer(p.m_Pointer) {
        p.m_Pointer = nullptr;
    }

    // 从原始指针构造
    SmartPointer(ObjectType* p) noexcept
        : m_Pointer(p) {
        this->Register();
    }

    // 析构函数
    ~SmartPointer() { this->UnRegister(); }

    // 操作符重载
    ObjectType* operator->() const noexcept { return m_Pointer; }
    ObjectType& operator*() const noexcept { return *m_Pointer; }
    explicit operator bool() const noexcept { return m_Pointer != nullptr; }
    operator ObjectType* () const noexcept { return m_Pointer; }

    // 赋值运算符（使用 copy-and-swap  idiom）
    SmartPointer& operator=(SmartPointer r) noexcept {
        this->Swap(r);
        return *this;
    }

    SmartPointer& operator=(std::nullptr_t) noexcept {
        this->UnRegister();
        this->m_Pointer = nullptr;
        return *this;
    }

    void Swap(SmartPointer& other) noexcept {
        ObjectType* tmp = this->m_Pointer;
        this->m_Pointer = other.m_Pointer;
        other.m_Pointer = tmp;
    }

private:
    ObjectType* m_Pointer{ nullptr };

    template <typename T>
    friend class SmartPointer;

    // 增加引用计数
    void Register() noexcept {
        if (m_Pointer) {
            m_Pointer->Register();
        }
    }

    // 减少引用计数
    void UnRegister() noexcept {
        if (m_Pointer) {
            m_Pointer->UnRegister();
        }
    }
};
```

### 关键点解析

#### 1. 构造函数

**默认构造**：
```cpp
constexpr SmartPointer() noexcept = default;
constexpr SmartPointer(std::nullptr_t) noexcept {}
```
- 创建空指针，不引用任何对象

**从原始指针构造**：
```cpp
SmartPointer(ObjectType* p) noexcept : m_Pointer(p) {
    this->Register();  // 立即增加引用计数
}
```
- **重要**：构造时立即调用 `Register()`，引用计数从0变为1

**拷贝构造**：
```cpp
SmartPointer(const SmartPointer& p) noexcept 
    : m_Pointer(p.m_Pointer) {
    this->Register();  // 新指针引用同一对象，计数+1
}
```

**移动构造**：
```cpp
SmartPointer(SmartPointer<ObjectType>&& p) noexcept
    : m_Pointer(p.m_Pointer) {
    p.m_Pointer = nullptr;  // 原指针置空，不调用 UnRegister
}
```
- 移动时不改变引用计数（所有权转移）

#### 2. 析构函数

```cpp
~SmartPointer() { this->UnRegister(); }
```
- **关键**：智能指针销毁时，自动调用 `UnRegister()`
- 如果这是最后一个引用，对象会被自动删除

#### 3. 操作符重载

```cpp
ObjectType* operator->() const noexcept { return m_Pointer; }
ObjectType& operator*() const noexcept { return *m_Pointer; }
operator ObjectType* () const noexcept { return m_Pointer; }
```
- 提供类似原始指针的使用方式
- 可以隐式转换为原始指针（用于兼容性）

#### 4. 赋值运算符（Copy-and-Swap Idiom）

```cpp
SmartPointer& operator=(SmartPointer r) noexcept {
    this->Swap(r);
    return *this;
}
```
- **参数是值传递**：自动处理自赋值和异常安全
- 先构造临时对象（会调用 `Register()`）
- 然后交换（旧指针会调用 `UnRegister()`）

#### 5. 类型转换支持

```cpp
template <typename T, typename = typename EnableIfConvertible<T>::type>
SmartPointer(const SmartPointer<T>& p) noexcept
    : m_Pointer(p.m_Pointer) {
    this->Register();
}
```
- 支持从派生类智能指针转换为基类智能指针
- 例如：`SmartPointer<DataObject>` 可以从 `SmartPointer<PointSet>` 构造

---

## Object - 完整对象系统

### 继承关系

```
AbstractObject (引用计数基础)
    ↓
Object (完整功能：时间戳、观察者模式)
    ↓
DataObject, Filter, Command 等（具体功能类）
```

### Object 类关键成员

```cpp
class Object : public AbstractObject {
public:
    I_OBJECT(Object);
    static Pointer New() { return new Object; }

    // 时间戳管理
    void Modified();
    TimeStamp& GetMTime() { return m_MTime; }

    // 名称管理
    void SetName(const std::string& name);
    const std::string& GetName() const;

    // 观察者模式（事件系统）
    unsigned long AddObserver(...);
    void RemoveObserver(...);
    bool InvokeEvent(...);

protected:
    ~Object() override;
    Object() {}

    TimeStamp m_MTime{};              // 修改时间戳
    ObserverInternal* m_Internal{};   // 观察者内部数据
    std::string m_Name{};             // 对象名称
};
```

### 关键特性

#### 1. 工厂模式
```cpp
static Pointer New() { return new Object; }
```
- **唯一创建方式**：只能通过 `New()` 创建对象
- 返回智能指针，确保内存安全
- 构造函数是 `protected`，防止直接创建

#### 2. 时间戳（TimeStamp）
```cpp
void Modified() {
    m_MTime.Modified();  // 更新时间戳
}
```
- **用途**：检测对象是否被修改
- **机制**：全局递增计数器，每次修改时更新
- **应用**：数据依赖检测、缓存失效判断

#### 3. 观察者模式
- 对象可以注册观察者（监听器）
- 当事件发生时，通知所有观察者
- 用于解耦对象之间的依赖关系

---

## 使用示例

### 示例1：基本使用

```cpp
// 1. 创建对象（使用工厂方法）
Object::Pointer obj = Object::New();
// 此时引用计数 = 1

// 2. 创建另一个引用
Object::Pointer obj2 = obj;
// 此时引用计数 = 2（两个智能指针都引用同一对象）

// 3. 修改对象
obj->SetName("MyObject");
obj->Modified();  // 更新时间戳

// 4. 使用对象
std::string name = obj->GetName();

// 5. 智能指针自动管理
// 当 obj 和 obj2 都销毁时，对象自动删除
```

### 示例2：类型转换

```cpp
// 假设 PointSet 继承自 DataObject
PointSet::Pointer pointSet = PointSet::New();

// 向上转换（派生类 -> 基类）
DataObject::Pointer dataObj = pointSet;  // 自动转换，引用计数不变

// 向下转换（基类 -> 派生类）
PointSet::Pointer pointSet2 = DynamicCast<PointSet>(dataObj);
if (pointSet2.IsNotNull()) {
    // 转换成功
}
```

### 示例3：函数参数和返回值

```cpp
// 函数参数（按值传递，自动管理引用计数）
void ProcessObject(Object::Pointer obj) {
    // obj 的引用计数在这里+1
    obj->SetName("Processed");
    // 函数结束时，obj 销毁，引用计数-1
}

// 函数返回值
Object::Pointer CreateObject() {
    Object::Pointer obj = Object::New();
    return obj;  // 返回值时引用计数不变（移动语义）
}

// 使用
Object::Pointer obj = CreateObject();
ProcessObject(obj);  // 传递时引用计数+1，函数返回后-1
```

### 示例4：容器中使用

```cpp
// 在容器中存储智能指针
std::vector<Object::Pointer> objects;

// 添加对象
objects.push_back(Object::New());  // 引用计数 = 2（vector 中一个，临时对象一个）
// 临时对象销毁后，引用计数 = 1

// 访问对象
if (!objects.empty()) {
    objects[0]->SetName("First");
}

// 清空容器
objects.clear();  // 所有智能指针销毁，对象自动删除
```

### 示例5：时间戳的使用

```cpp
DataObject::Pointer obj = DataObject::New();
TimeStamp oldTime = obj->GetMTime();

// 修改对象
obj->Modified();
TimeStamp newTime = obj->GetMTime();

// 检查是否被修改
if (newTime > oldTime) {
    std::cout << "Object was modified!" << std::endl;
}

// 在算法中检查依赖
void MyFilter::Execute() {
    DataObject::Pointer input = GetInput(0);
    if (input->GetMTime() > m_LastExecuteTime) {
        // 输入数据已更新，需要重新计算
        Recompute();
        m_LastExecuteTime = input->GetMTime();
    }
}
```

---

## 设计模式分析

### 1. 引用计数模式（Reference Counting）

**优点**：
- 自动内存管理，防止内存泄漏
- 多个对象可以安全共享同一资源
- 不需要手动 `delete`

**缺点**：
- 循环引用问题（需要 `weak_ptr` 或手动打破循环）
- 引用计数操作有性能开销（但很小）

### 2. 工厂模式（Factory Pattern）

**实现**：
```cpp
static Pointer New() { return new Object; }
```

**优点**：
- 统一创建接口
- 强制使用智能指针
- 可以添加创建前的初始化逻辑

### 3. RAII（Resource Acquisition Is Initialization）

**体现**：
- 智能指针在构造时获取资源（调用 `Register()`）
- 在析构时释放资源（调用 `UnRegister()`）
- 异常安全：即使发生异常，资源也会被正确释放

### 4. Copy-and-Swap Idiom

**实现**：
```cpp
SmartPointer& operator=(SmartPointer r) noexcept {
    this->Swap(r);
    return *this;
}
```

**优点**：
- 自动处理自赋值
- 异常安全
- 代码简洁

---

## 常见问题和注意事项

### Q1: 为什么构造函数是 protected？

**A**: 防止直接创建对象，强制使用 `New()` 工厂方法，确保返回智能指针。

### Q2: 可以手动 delete 对象吗？

**A**: 不建议。应该让智能指针自动管理。如果确实需要，可以使用 `Delete()`，但要注意所有引用都会变成悬空指针。

### Q3: 循环引用怎么办？

**A**: 
- 使用原始指针存储"不拥有所有权"的引用
- 或者手动打破循环（在析构前调用 `UnRegister()`）

### Q4: 性能影响？

**A**: 
- 引用计数操作是原子操作，开销很小
- 现代 CPU 对原子操作优化很好
- 相比手动内存管理，性能损失可忽略

### Q5: 线程安全吗？

**A**: 
- 引用计数操作是线程安全的（使用 `std::atomic`）
- 但对象本身的访问需要额外的同步机制

---

## 总结

1. **I_OBJECT 宏**：禁止拷贝，定义类型别名
2. **AbstractObject**：提供引用计数基础功能
3. **SmartPointer**：智能指针实现，自动管理引用计数
4. **Object**：完整对象系统，包含时间戳和观察者模式

**核心流程**：
```
创建对象 → New() → 返回 SmartPointer → Register() → 引用计数=1
使用对象 → 多个 SmartPointer 引用 → 引用计数增加
销毁引用 → SmartPointer 析构 → UnRegister() → 引用计数减少
计数为0 → 自动 delete 对象
```

这个设计确保了内存安全，是 iGameVis 框架的基础。
