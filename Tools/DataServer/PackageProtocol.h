#pragma once

// Compatibility include for standalone DataServer users. The protocol is
// shared with the desktop client from the core DataTransfer directory.
#include "DataTransfer/iGamePackageProtocol.h"
